apiVersion: v1
kind: Service
metadata:
  name: {{ include "kaushal-mental-models.fullname" . }}-service
  labels:
    {{- include "kaushal-mental-models.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "kaushal-mental-models.selectorLabels" . | nindent 4 }}
