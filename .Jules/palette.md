## 2024-01-21 - [Accessibility: Icon-Only Buttons]
**Learning:** Icon-only buttons (like edit/delete actions) are invisible to screen readers without explicit labels. Using `aria-label` provides necessary context.
**Action:** Always verify icon-only buttons have descriptive `aria-label` attributes or `sr-only` text spans.
